[package]
name = "recipe-transport"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Bounded master and worker communication for Recipe"
repository = "https://github.com/nm-z/nates-recipe-rs"
readme = "README.md"
keywords = ["gpu", "networking", "distributed"]
categories = ["network-programming"]

[dependencies]
recipe-core = { path = "../core" }
recipe-probe = { path = "../probe" }
sha2 = "0.10.9"
