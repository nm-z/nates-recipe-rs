[package]
name = "recipe-kernel"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
recipe-core = { path = "../recipe-core" }
recipe-language = { path = "../recipe-language" }
recipe-primitives = { path = "../recipe-primitives" }
sha2 = "0.10"
