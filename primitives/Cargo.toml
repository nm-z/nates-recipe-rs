[package]
name = "recipe-primitives"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Deterministic AOT lowering of Recipe tensor primitives into GPU stage programs"
repository.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
recipe-core = { path = "../core" }
recipe-language = { path = "../language" }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "deny"
