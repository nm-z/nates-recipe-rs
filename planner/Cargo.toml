[package]
name = "recipe-planner"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Deterministic measured-topology Draft candidate planner for Recipe"
repository.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
recipe-core = { path = "../core" }
recipe-language = { path = "../language" }
recipe-primitives = { path = "../primitives" }
recipe-program = { path = "../program" }
recipe-scheduler = { path = "../scheduler" }
sha2 = "0.10.9"

[lints.rust]
unsafe_code = "forbid"
