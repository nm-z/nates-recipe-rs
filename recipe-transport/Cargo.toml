[package]
name = "recipe-transport"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
recipe-core = { path = "../recipe-core" }
recipe-probe = { path = "../recipe-probe" }
sha2 = "0.10.9"
