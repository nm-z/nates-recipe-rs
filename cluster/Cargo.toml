[package]
name = "recipe-cluster"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Measured multi-machine profile assembly for Recipe"
repository = "https://github.com/nm-z/nates-recipe-rs"
readme = "README.md"
keywords = ["gpu", "cluster", "distributed"]
categories = ["science"]

[dependencies]
recipe-core = { path = "../core" }
recipe-probe = { path = "../probe" }
recipe-transport = { path = "../transport" }
sha2 = "0.10.9"

[lints.rust]
unsafe_code = "forbid"
