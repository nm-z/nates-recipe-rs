[package]
name = "recipe-hsa"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Reviewed ROCr/HSA discovery and queue ownership for Recipe"

[dependencies]
libc = "0.2"

[features]
live-hsa = []

[[example]]
name = "execute_smoke"
required-features = ["live-hsa"]

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"

[lints.clippy]
undocumented_unsafe_blocks = "deny"
