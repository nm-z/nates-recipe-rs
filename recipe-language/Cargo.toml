[package]
name = "recipe-language"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Backend-neutral tensor and primitive-kernel language for Recipe"

[dependencies]
recipe-core = { path = "../recipe-core" }

[lints.rust]
unsafe_code = "forbid"
