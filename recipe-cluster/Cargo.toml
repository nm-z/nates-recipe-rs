[package]
name = "recipe-cluster"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
recipe-core = { path = "../recipe-core" }
recipe-probe = { path = "../recipe-probe" }
recipe-transport = { path = "../recipe-transport" }
sha2 = "0.10.9"

[lints.rust]
unsafe_code = "forbid"
