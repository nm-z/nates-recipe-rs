[package]
name = "recipe-ops"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Canonical operation registry and honest lowering boundary for Recipe"
build = "build.rs"

[dependencies]
recipe-core = { path = "../recipe-core" }
recipe-language = { path = "../recipe-language" }
recipe-math = { path = "../recipe-math" }
recipe-primitives = { path = "../recipe-primitives" }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "deny"
