[package]
name = "gpu-core"
version = "0.1.0"
edition = "2024"

[dependencies]
half = "2"
libc = "0.2"

[dev-dependencies]
serde_json = "1"
libm = "0.2"
rusqlite = { version = "0.32", features = ["bundled"] }
