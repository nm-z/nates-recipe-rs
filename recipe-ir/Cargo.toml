[package]
name = "recipe-ir"
version = "0.1.0"
edition = "2024"
description = "Backend-free model IR: layer, loss, metric, parameter, shape, and work definitions"
license = "MIT"
repository = "https://github.com/nm-z/nates-recipe-rs"
readme = "../README.md"
keywords = ["ir", "model", "layers", "machine-learning"]
categories = ["science::ml", "data-structures"]

[lib]
name = "recipe_ir"

[lints]
workspace = true
