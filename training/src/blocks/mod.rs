/// The `mod` variant.
mod attention;
/// The `mod` variant.
mod convolution;
/// The `mod` variant.
mod embedding;
/// The `mod` variant.
mod gru;
/// The `mod` variant.
mod kmeans;
/// The `mod` variant.
mod layer;
/// The `mod` variant.
mod lstm;
/// The `mod` variant.
mod pool;
/// The `mod` variant.
mod residual;
/// The `mod` variant.
mod rnn;
/// The `mod` variant.
mod tree;
