RECIPE QUEUED FAILURE BEGIN
id=a10a77a8268bbeab
base=commit=38b4e1f2da11d0aade75f515bcac884c73d044c6 tracked_tree=clean
cursor=seed:0 cursor:80 next:81 step:53196099145139 composition:123498698648560
data=path:"data/numeric/semicolon_data.data" hash:d8be9801897ff075
configuration=data_mode:2 model:recipe.model().bayes().tan().qi(4).0.attn(1).prelu().qi(6).k.loss(rmse) loss:1 arithmetic:1 stop:2 rate:1 lifecycle:1
expected=training, optional resume, and inference produce finite numerical results through the public Recipe API
observed=phase:training message:device amd0 produced a nonfinite prediction
output=phase:training message:device amd0 produced a nonfinite prediction
replay=phase:training message:device amd0 produced a nonfinite prediction stable:true
command=cargo run --bin recipe -- /tmp/recipe-composition-repro.rs
reproduction:
```rust
use recipe::*;

fn main() {
	let bundle = "/tmp/recipe-composition-repro.ogdl";
	let data = recipe.data("data/numeric/semicolon_data.data")
		.target("target")
		.split(0.8);
	let model = recipe.model()
		.bayes().tan().qi(4).0
		.attn(1).prelu().qi(6).k
		.loss(rmse);
	let report = recipe.train()
		.optimizer(adamw)
		.lr(0.0001)
		.seed(123498698648560)
		.epochs(1)
		.log(all)
		.stop(0.8)
		.fp(16)
		.save(bundle)
		.run(&model, &data);
	assert!(report.final_loss().is_finite());
}
```
RECIPE QUEUED FAILURE END
RECIPE QUEUED FAILURE BEGIN
id=dbd897973cf7a17c
base=commit=38b4e1f2da11d0aade75f515bcac884c73d044c6 tracked_tree=clean
cursor=seed:0 cursor:81 next:82 step:53196099145139 composition:43398370923939
data=path:"data/categoric/archive_zip.zip" hash:8cbca5e6fcb5bad8
configuration=data_mode:3 model:recipe.model().layer(8).moe(1, [layer(8), layer(8)]).tanh().qi(8).0.attn(1).qi(4).k.m.loss(mse) loss:0 arithmetic:9 stop:0 rate:1 lifecycle:0
expected=training, optional resume, and inference produce finite numerical results through the public Recipe API
observed=phase:training message:data source contains no supported table
output=phase:training message:data source contains no supported table
replay=phase:training message:data source contains no supported table stable:true
command=cargo run --bin recipe -- /tmp/recipe-composition-repro.rs
reproduction:
```rust
use recipe::*;

fn main() {
	let bundle = "/tmp/recipe-composition-repro.ogdl";
	let data = recipe.data("data/categoric/archive_zip.zip")
		.target("target")
		.broadcast();
	let model = recipe.model()
		.layer(8)
		.moe(1, [layer(8), layer(8)]).tanh().qi(8).0
		.attn(1).qi(4).k.m
		.loss(mse);
	let report = recipe.train()
		.optimizer(adamw)
		.lr(0.0001)
		.seed(43398370923939)
		.epochs(1)
		.log(all)
		.f(6, 9)
		.save(bundle)
		.run(&model, &data);
	assert!(report.final_loss().is_finite());
}
```
RECIPE QUEUED FAILURE END
RECIPE QUEUED FAILURE BEGIN
id=442313ea94609d22
base=commit=38b4e1f2da11d0aade75f515bcac884c73d044c6 tracked_tree=clean
cursor=seed:0 cursor:86 next:87 step:53196099145139 composition:42786012910114
data=path:"data/text/documents_mixed" hash:484b7d4f804d5f26
configuration=data_mode:2 model:recipe.model().knn(3).cos().iq(2).m.lgbm().huber().norm(batch).qi(4).nf.loss(mae) loss:3 arithmetic:8 stop:0 rate:1 lifecycle:0
expected=training, optional resume, and inference produce finite numerical results through the public Recipe API
observed=phase:training message:target "target" must identify exactly one feature
output=phase:training message:target "target" must identify exactly one feature
replay=phase:training message:target "target" must identify exactly one feature stable:true
command=cargo run --bin recipe -- /tmp/recipe-composition-repro.rs
reproduction:
```rust
use recipe::*;

fn main() {
	let bundle = "/tmp/recipe-composition-repro.ogdl";
	let data = recipe.data("data/text/documents_mixed")
		.target("target")
		.split(0.8);
	let model = recipe.model()
		.knn(3).cos().iq(2).m
		.lgbm().huber().norm(batch).qi(4).nf
		.loss(mae);
	let report = recipe.train()
		.optimizer(adamw)
		.lr(0.0001)
		.seed(42786012910114)
		.epochs(1)
		.log(all)
		.tf(32)
		.save(bundle)
		.run(&model, &data);
	assert!(report.final_loss().is_finite());
}
```
RECIPE QUEUED FAILURE END
RECIPE QUEUED FAILURE BEGIN
id=ff19adddc738279e
base=commit=38b4e1f2da11d0aade75f515bcac884c73d044c6 tracked_tree=clean
cursor=seed:0 cursor:92 next:93 step:53196099145139 composition:95369754041428
data=path:"data/image/paired_images" hash:8200ee4b7ec24191
configuration=data_mode:0 model:recipe.model().pool(2).gelu().norm(batch).qi(5).k.lgbm().selu().norm(batch).iq(4).nl.loss(ce) loss:5 arithmetic:5 stop:2 rate:0 lifecycle:1
expected=training, optional resume, and inference produce finite numerical results through the public Recipe API
observed=phase:training message:paired target directory "target" holds images, not values
output=phase:training message:paired target directory "target" holds images, not values
replay=phase:training message:paired target directory "target" holds images, not values stable:true
command=cargo run --bin recipe -- /tmp/recipe-composition-repro.rs
reproduction:
```rust
use recipe::*;

fn main() {
	let bundle = "/tmp/recipe-composition-repro.ogdl";
	let data = recipe.data("data/image/paired_images")
		.target("target");
	let model = recipe.model()
		.pool(2).gelu().norm(batch).qi(5).k
		.lgbm().selu().norm(batch).iq(4).nl
		.loss(ce);
	let report = recipe.train()
		.optimizer(adamw)
		.lr(0.001)
		.seed(95369754041428)
		.epochs(1)
		.log(all)
		.stop(0.8)
		.int(4)
		.save(bundle)
		.run(&model, &data);
	assert!(report.final_loss().is_finite());
}
```
RECIPE QUEUED FAILURE END
RECIPE QUEUED FAILURE BEGIN
id=bb10085e9d7ef5c8
base=commit=38b4e1f2da11d0aade75f515bcac884c73d044c6 tracked_tree=clean
cursor=seed:0 cursor:93 next:94 step:53196099145139 composition:15269426316807
data=path:"data/text/autoregressive_lines.txt" hash:d0107c1d38bcac89
configuration=data_mode:3 model:recipe.model().rnn(8).iq(1).m.xgbst().gelu().qi(2).k.loss(ce) loss:5 arithmetic:3 stop:1 rate:0 lifecycle:0
expected=training, optional resume, and inference produce finite numerical results through the public Recipe API
observed=phase:training message:dataset /tmp/recipe-counterexample-machine/data/text/autoregressive_lines.txt has 9412 rows differing from the expected 2 fields
output=phase:training message:dataset /tmp/recipe-counterexample-machine/data/text/autoregressive_lines.txt has 9412 rows differing from the expected 2 fields
replay=phase:training message:dataset /tmp/recipe-counterexample-machine/data/text/autoregressive_lines.txt has 9412 rows differing from the expected 2 fields stable:true
command=cargo run --bin recipe -- /tmp/recipe-composition-repro.rs
reproduction:
```rust
use recipe::*;

fn main() {
	let bundle = "/tmp/recipe-composition-repro.ogdl";
	let data = recipe.data(auto)
		.set("data/text/autoregressive_lines.txt")
		.broadcast();
	let model = recipe.model()
		.rnn(8).iq(1).m
		.xgbst().gelu().qi(2).k
		.loss(ce);
	let report = recipe.train()
		.optimizer(adamw)
		.lr(0.001)
		.seed(15269426316807)
		.epochs(1)
		.log(all)
		.stop(0.0)
		.fp(64)
		.save(bundle)
		.run(&model, &data);
	assert!(report.final_loss().is_finite());
}
```
RECIPE QUEUED FAILURE END
RECIPE QUEUED FAILURE BEGIN
id=66504912e098d738
base=commit=38b4e1f2da11d0aade75f515bcac884c73d044c6 tracked_tree=clean
cursor=seed:0 cursor:94 next:95 step:53196099145139 composition:68465525461946
data=path:"data/numeric/records_jsonl.jsonl" hash:03dfc5c27ea1ecff
configuration=data_mode:0 model:recipe.model().conv(8, 3).huber().iq(2).m.xgbst().silu().norm(batch).qi(4).k.loss(bce) loss:4 arithmetic:1 stop:0 rate:0 lifecycle:1
expected=training, optional resume, and inference produce finite numerical results through the public Recipe API
observed=phase:training message:convolution kernel exceeds sequence length
output=phase:training message:convolution kernel exceeds sequence length
replay=phase:training message:convolution kernel exceeds sequence length stable:true
command=cargo run --bin recipe -- /tmp/recipe-composition-repro.rs
reproduction:
```rust
use recipe::*;

fn main() {
	let bundle = "/tmp/recipe-composition-repro.ogdl";
	let data = recipe.data("data/numeric/records_jsonl.jsonl")
		.target("target");
	let model = recipe.model()
		.conv(8, 3).huber().iq(2).m
		.xgbst().silu().norm(batch).qi(4).k
		.loss(bce);
	let report = recipe.train()
		.optimizer(adamw)
		.lr(0.001)
		.seed(68465525461946)
		.epochs(1)
		.log(all)
		.fp(16)
		.save(bundle)
		.run(&model, &data);
	assert!(report.final_loss().is_finite());
}
```
RECIPE QUEUED FAILURE END
RECIPE QUEUED FAILURE BEGIN
id=3c06cf7e6dc818ac
base=commit=38b4e1f2da11d0aade75f515bcac884c73d044c6 tracked_tree=clean
cursor=seed:0 cursor:95 next:96 step:53196099145139 composition:121661624607085
data=path:"data/text/single_tsv.tsv" hash:f788e68b9a4ad7c8
configuration=data_mode:3 model:recipe.model().attn(1).sigmoid().iq(3).m.xgbst().elu().iq(1).m.loss(mae) loss:3 arithmetic:9 stop:1 rate:1 lifecycle:1
expected=training, optional resume, and inference produce finite numerical results through the public Recipe API
observed=phase:training message:native contraction tile does not fit the device
output=phase:training message:native contraction tile does not fit the device
replay=phase:training message:native contraction tile does not fit the device stable:true
command=cargo run --bin recipe -- /tmp/recipe-composition-repro.rs
reproduction:
```rust
use recipe::*;

fn main() {
	let bundle = "/tmp/recipe-composition-repro.ogdl";
	let data = recipe.data("data/text/single_tsv.tsv")
		.target("target")
		.broadcast();
	let model = recipe.model()
		.attn(1).sigmoid().iq(3).m
		.xgbst().elu().iq(1).m
		.loss(mae);
	let report = recipe.train()
		.optimizer(adamw)
		.lr(0.0001)
		.seed(121661624607085)
		.epochs(1)
		.log(all)
		.stop(0.0)
		.f(6, 9)
		.save(bundle)
		.run(&model, &data);
	assert!(report.final_loss().is_finite());
}
```
RECIPE QUEUED FAILURE END
RECIPE QUEUED FAILURE BEGIN
id=dbd897973cf7a17c
base=commit=38b4e1f2da11d0aade75f515bcac884c73d044c6 tracked_tree=modified
cursor=seed:0 cursor:110 next:111 step:53196099145139 composition:119824550565610
data=path:"data/numeric/single_xlsx.xlsx" hash:1ec4de78eae15f09
configuration=data_mode:2 model:recipe.model().layer(8).residual([conv(8, 1), conv(8, 1)]).gelu().iq(2).m.bayes().silu().qi(5).0.loss(focal) loss:6 arithmetic:7 stop:1 rate:1 lifecycle:1
expected=training, optional resume, and inference produce finite numerical results through the public Recipe API
observed=phase:training message:data source contains no supported table
output=phase:training message:data source contains no supported table
replay=phase:training message:data source contains no supported table stable:true
command=RECIPE_FORCE_CPU=1 cargo run --bin recipe -- /tmp/recipe-composition-repro-2381540.rs
reproduction:
```rust
use recipe::*;

fn main() {
	let bundle = "/tmp/recipe-composition-repro.ogdl";
	let data = recipe.data("data/numeric/single_xlsx.xlsx")
		.target("target")
		.split(0.8);
	let model = recipe.model()
		.layer(8)
		.residual([conv(8, 1), conv(8, 1)]).gelu().iq(2).m
		.bayes().silu().qi(5).0
		.loss(focal);
	let report = recipe.train()
		.optimizer(adamw)
		.lr(0.0001)
		.seed(119824550565610)
		.epochs(1)
		.log(all)
		.stop(0.0)
		.bf(16)
		.save(bundle)
		.run(&model, &data);
	assert!(report.final_loss().is_finite());
}
```
backend=cpu
RECIPE QUEUED FAILURE END
