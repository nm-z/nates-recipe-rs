[package]
name = "recipe-infer"
version = "0.1.0"
edition = "2024"
description = "GPU forward-pass inference: pure tensor fns, ogdl/safetensors weight loading, device lifecycle"
license = "MIT"
repository = "https://github.com/nm-z/nates-recipe-rs"
readme = "../README.md"
keywords = ["inference", "gpu", "hip", "safetensors", "tensor"]
categories = ["science::ml"]

[lib]
name = "recipe_infer"

[dependencies]
anyhow = "1"
libc = "0.2"
gpu-core = { path = "../gpu-core" }
half = "2"
tokenizers = "0.21"
ndarray = "0.16"
ogdl = { path = "../ogdl" }
minijinja = "2"
hf-chat-template = { version = "0.2", features = ["pycompat"] }

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true
