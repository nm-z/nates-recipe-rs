recipe
	.data()
		.set(source)
		.target(targets)
		.exclude(exclusions)
		.norm(z_score)
		.norm(min_max)
		.norm(l2_norm)
		.split(train_fraction)
	.data(source)
		.set(source)
		.target(targets)
		.exclude(exclusions)
		.norm(z_score)
		.norm(min_max)
		.norm(l2_norm)
		.split(train_fraction)
	.data([sources])
		.set(source)
		.target(targets)
		.exclude(exclusions)
		.norm(z_score)
		.norm(min_max)
		.norm(l2_norm)
		.split(train_fraction)
	.model()
		.load(path)
		.bayes(child, [parents])
		.perc(count)
		.layer(neurons)
		.conv(filters, kernel)
		.pool(size)
			.layer(neurons)
		.kmeans(clusters)
			.layer(neurons)
		.knn(neighbors)
		.knn(outputs, neighbors)
		.residual(layer(neurons), relu(), ...)
		.lgbm(depth)
		.cbst(depth)
		.xgbst(depth)
		.forest(trees)
			.lgbm(depth)
			.cbst(depth)
			.xgbst(depth)
		.embed(dimensions)
			.vocab(vocabulary)
		.attn(heads)
		.rnn(width)
		.gru(width)
		.lstm(width)
		.norm(layer_norm)
		.norm(batch_norm)
		.relu()
		.leak()
		.sigmoid()
		.tanh()
		.tan()
		.selu()
		.gelu()
		.silu()
		.elu()
		.prelu()
		.cos()
		.exp()
		.log()
		.huber()
		.loss(bce)
		.loss(mse)
		.loss(mae)
		.loss(ce)
		.loss(huber)
		.loss(focal)
		.grad(clip: maximum_norm)
	.train()
		.optimizer(adamw)
		.batch(fraction)
		.epochs(count)
		.lr(rate)
			.cos()
			.exp()
		.warmup(count)
		.early_stop(metric, patience)
		.log(Loss)
		.log(Accuracy)
		.log(R2)
		.log(AuRoc)
		.log(AuPrc)
		.log(Brier)
		.log(CalibrationError)
		.log(Epoch)
		.log(Lr)
		.log(Time)
		.log(Device)
		.log([items])
		.log_every(interval)
		.plot([items])
		.resume(path)
		.run()
			.save(path)
	.infer()
		.log(item)
		.log([items])
		.evaluate()
