recipe
	.data(sources)
		.set(source)
		.target(targets)
		.exclude(exclusions)
		.norm(data_normalization)
		.split(train_fraction)
	.model()
		.load(weights, input_width)
		.bayes(child, parents)
		.perc(count)
		.layer(neurons)
		.conv(filters, kernel)
		.pool(size)
			.layer(neurons)
		.kmeans(clusters)
			.layer(neurons)
		.knn(neighbors)
		.knn(outputs, neighbors)
		.residual(branch_operations)
		.lgbm(depth)
		.cbst(depth)
		.xgbst(depth)
		.forest(trees)
			.lgbm(depth)
			.cbst(depth)
			.xgbst(depth)
		.embed(dimensions)
			.vocab(vocabulary)
		.attn(heads)
		.rnn(width)
		.gru(width)
		.lstm(width)
		.norm(layer_normalization)
		.relu()
		.leak()
		.sigmoid()
		.tanh()
		.tan()
		.selu()
		.gelu()
		.silu()
		.elu()
		.prelu()
		.cos()
		.exp()
		.log()
		.huber()
		.loss(objective)
		.grad(clip(maximum_norm))
	.train()
		.optimizer(optimizer)
		.batch(fraction)
		.epochs(count)
		.lr(rate)
			.cos()
			.exp()
		.warmup(count)
		.early_stop(metric, patience)
		.log(items)
		.log_every(interval)
		.plot(items)
		.resume(path)
		.run()
			.save(path)
	.infer()
		.log(items)
		.evaluate(data, model)

	.residual(
		layer(64),
		relu(),
		...
	).layer(1).loss(mse)...
